<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.polito.mad.mad_lab2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="it.polito.mad.mad_lab2.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="it.polito.mad.mad_lab2.ChoiceActivity"
            android:label="@string/app_name" >
        </activity>

        <provider
            android:name="it.polito.mad.mad_lab2.util.FileProvider"
            android:authorities="it.polito.mad.mad_lab2.util"
            android:exported="true" >
        </provider>

        <activity
            android:name="it.polito.mad.mad_lab2.SplashActivity"
            android:label="@string/title_activity_splash" >
        </activity>
    </application>

</manifest>
